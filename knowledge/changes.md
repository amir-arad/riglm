## in Endpoints:
 - no authentication or API key
 - no custom URL
 - instead of status, just active/inactive
 - endpoint name should be a slug , legal url path element.

## in Servers:
 - instead of status, just active/inactive



each endpoint should have its own page